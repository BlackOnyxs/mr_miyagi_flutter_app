import 'package:flutter/material.dart';

class AddressSettingView extends StatelessWidget {
  const AddressSettingView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hi AddressSettingView '),
      ),
    );
  }
}